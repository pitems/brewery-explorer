import 'package:flutter/material.dart';

class {{generatedClassName}} extends StatelessWidget {
  const {{generatedClassName}}({super.key});

  @override
  Widget build(BuildContext context) {
    return {{body}};
  }
}
